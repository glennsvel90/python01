
while True:
	#this code will run forever
	input = raw_input("write stuff: ")
	print input

	#if the input is done, it will break out of the forever
	if input == "done":
		break

	#if the input is anything else it will stay forever	
	else:
		continue

